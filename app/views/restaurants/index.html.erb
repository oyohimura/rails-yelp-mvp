<div class="container">
  <h1>Liste de tous les restaurants disponibles :</h1>
  <% @restaurants.each do |restaurant| %>
    <p><%= restaurant.name %>
    <%= link_to 'See more info', restaurant_path(restaurant) %></p>
  <% end %>

  <%= link_to 'Create new restaurant', new_restaurant_path %>
</div>
